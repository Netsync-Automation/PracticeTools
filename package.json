{
  "name": "practicetools",
  "version": "1.0.0",
  "description": "PracticeTools application",
  "main": "server.js",
  "scripts": {
    "build": "npm install && npm run build",
    "start": "npm run start",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}